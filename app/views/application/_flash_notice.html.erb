<div class="flash">
  <% flash.each do |type, message| -%>
    <div class="alert <%= flash_klass(type) %>">
      <button type="button" class="close" data-dismiss="alert">×</button>
      <%= message %>
    </div>
  <% end -%>
</div>
